import 'package:flutter/material.dart';

class Kitap_Liste extends StatelessWidget {
  const Kitap_Liste({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(""),
    );
  }
}
